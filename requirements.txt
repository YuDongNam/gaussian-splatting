# Core dependencies
plyfile>=0.7.4
numpy>=1.21.0
pandas>=1.3.0
torch>=1.12.0

# Statistical analysis dependencies
matplotlib>=3.5.0
seaborn>=0.11.0
statsmodels>=0.13.0
pygam>=0.9.0

# Submodules (install from local paths)
# Note: These need to be installed separately using:
# pip install submodules/diff-gaussian-rasterization
# pip install submodules/simple-knn
# pip install submodules/fused-ssim

